create table bank(
bank_id number,
bank_name varchar2(20),
opned_date date,
branch_code char(5)
);

insert into bank values
(101,'SBI',To_date('2025-02-25','YYYY-MM-DD'),'SB001');

insert into bank values
(102,'HDFC',To_date('2025-02-25','YYYY-MM-DD'),'HD002');

select * from bank;

create table bank(
bank_id number,
bank_name varchar2(20),
opned_date date,
branch_code char(5)
);

insert into bank values
(101,'SBI',To_date('2025-02-25','YYYY-MM-DD'),'SB001');

insert into bank values
(102,'HDFC',To_date('2025-02-25','YYYY-MM-DD'),'HD002');

-- All command are not case sensitive.
-- all clauses are keywords are not case sensistive.
-- Data types are not case sensitive.
--colum names are not case sensitive.
--table name are not case sensitive.
-- data is case sensitive